"""
The dashboard lives in dashboard/Load.py, where Streamlit's page discovery
can find the dashboard/pages/ subdirectory correctly.

Launch with:
    streamlit run dashboard/Load.py
"""
import sys

print("Launch the dashboard with: streamlit run dashboard/Load.py")
sys.exit(0)
